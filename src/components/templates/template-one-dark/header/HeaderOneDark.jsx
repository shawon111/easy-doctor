import NavbarOneDark from "./NavbarOneDark";

const HeaderOneDark = ({ content, page, slug }) => {
  return <NavbarOneDark content={content} page={page} slug={slug} />;
};

export default HeaderOneDark;
