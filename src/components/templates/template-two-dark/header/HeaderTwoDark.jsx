import NavbarTwoDark from "./NavbarTwoDark";

const HeaderTwoDark = ({ page, slug, content }) => {
  return <NavbarTwoDark page={page} slug={slug} content={content} />;
};

export default HeaderTwoDark;
